# Recursion `0.2.0-alpha.1`

Recursion `0.2.0-alpha.1` begins the alpha release line. It carries forward the current V1 runtime, Custom Decks & Cards, Post-Processing, provider routing, recovery behavior, and live-host verification work from the pre-alpha checkpoints while establishing the alpha contract as the active version.

## Release boundary

- The active package and extension manifest version is `0.2.0-alpha.1`.
- Persisted runtime records identify the current Recursion contract as `0.2.0-alpha.1`.
- Earlier `0.1.0-pre-alpha.*` notes remain historical checkpoints.
