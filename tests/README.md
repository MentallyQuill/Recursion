# Tests

Tests should start with deterministic module contracts before live SillyTavern smoke tests.

Expected early coverage:

- Scene fingerprinting and cache invalidation.
- Prompt-plan validation.
- Relevance scoring and budget omission reasons.
- Structured provider response parsing.
- Prompt packet installation and clearing with fake host adapters.
