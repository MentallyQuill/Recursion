# Editorial transformation visual baselines

Playwright owns these baselines. Run the no-generation matrix with
`EDITORIAL_UI_VISUAL_BASELINES=1` against a dedicated served host. Review every
desktop and compact-phone image before accepting a baseline update. Never store
chat text, provider output, or screenshots from generation-enabled runs.
